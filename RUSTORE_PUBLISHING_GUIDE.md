# 🚀 Полная инструкция по публикации Studyfay в RuStore

## 📋 Что нужно знать перед началом

RuStore — это российский магазин приложений от VK. Для публикации нужно:
- APK или AAB файл (до 5 ГБ)
- Цифровая подпись приложения
- Заполненная карточка приложения
- Скриншоты и описание

**Важно:** У нас уже есть готовое веб-приложение на React. Нам нужно упаковать его в Android-приложение.

---

## 🔧 ЧАСТЬ 1: Упаковка веб-приложения в APK

### Шаг 1: Установка необходимых инструментов

**Что нужно установить:**
1. **Node.js и npm** (уже должны быть установлены)
2. **Android Studio** — для сборки APK
   - Скачать: https://developer.android.com/studio
   - Установить, запустить один раз, дождаться установки SDK
3. **Java JDK 11+** (нужен для Android Studio)

**Проверка установки:**
```bash
node -v
npm -v
java -version
```

---

### Шаг 2: Установка Capacitor

Capacitor — это инструмент, который превращает веб-приложение в нативное Android-приложение.

**В терминале проекта выполните:**
```bash
npm install @capacitor/core @capacitor/cli @capacitor/android
```

**Инициализация Capacitor:**
```bash
npx cap init
```

Когда спросит — введите:
- **App name:** `Studyfay`
- **Package ID:** `dev.poehali.studyfay`
- **Web directory:** `dist`

---

### Шаг 3: Настройка capacitor.config.ts

Файл `capacitor.config.ts` уже создан в проекте. Проверьте, что он выглядит так:

```typescript
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.poehali.studyfay',
  appName: 'Studyfay',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
    cleartext: false,
    hostname: 'app.studyfay.ru'
  },
  android: {
    allowMixedContent: false,
    captureInput: true,
    webContentsDebuggingEnabled: false
  },
  plugins: {
    SplashScreen: {
      launchShowDuration: 2000,
      backgroundColor: "#8B5CF6",
      showSpinner: false
    },
    PushNotifications: {
      presentationOptions: ["badge", "sound", "alert"]
    }
  }
};

export default config;
```

---

### Шаг 4: Сборка веб-приложения

**Соберите production-версию:**
```bash
npm run build
```

Это создаст папку `dist/` с готовым приложением.

---

### Шаг 5: Добавление Android платформы

**Добавьте Android проект:**
```bash
npx cap add android
```

Это создаст папку `android/` с Android-проектом.

**Синхронизация (копирование dist в Android):**
```bash
npx cap sync
```

---

### Шаг 6: Создание ключа для подписи APK

**ОЧЕНЬ ВАЖНО:** Этот ключ нужно сохранить! Без него вы не сможете обновить приложение в будущем.

**Создайте ключ командой:**
```bash
keytool -genkey -v -keystore release-key.keystore -alias studyfay -keyalg RSA -keysize 2048 -validity 10000
```

**Что спросит команда:**
- **Пароль keystore:** Придумайте надёжный пароль (ЗАПИШИТЕ ЕГО!)
- **Имя и фамилия:** Ваше имя
- **Организация:** Studyfay или ваша компания
- **Город, область, страна:** Ваши данные
- **Правильно ли всё?** → `да`

**Результат:** Создастся файл `release-key.keystore` в корне проекта.

⚠️ **СОХРАНИТЕ ЭТИ ДАННЫЕ В НАДЁЖНОМ МЕСТЕ:**
- Пароль keystore
- Файл `release-key.keystore`
- Alias: `studyfay`

---

### Шаг 7: Настройка подписи в Android проекте

**Откройте файл:** `android/app/build.gradle`

**Найдите секцию `android { ... }` и добавьте:**

```gradle
android {
    namespace "dev.poehali.studyfay"
    compileSdkVersion 34
    
    defaultConfig {
        applicationId "dev.poehali.studyfay"
        minSdkVersion 22
        targetSdkVersion 34
        versionCode 1
        versionName "1.0.0"
    }
    
    signingConfigs {
        release {
            storeFile file("../../release-key.keystore")
            storePassword "ВАШ_ПАРОЛЬ_KEYSTORE"
            keyAlias "studyfay"
            keyPassword "ВАШ_ПАРОЛЬ_KEYSTORE"
        }
    }
    
    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.release
        }
    }
}
```

**Замените:** `ВАШ_ПАРОЛЬ_KEYSTORE` на реальный пароль, который вы создали в Шаге 6.

---

### Шаг 8: Сборка подписанного APK

**Перейдите в папку android:**
```bash
cd android
```

**Соберите release APK:**
```bash
./gradlew assembleRelease
```

(На Windows: `gradlew.bat assembleRelease`)

**Процесс займёт 2-5 минут.** Дождитесь сообщения `BUILD SUCCESSFUL`.

**Готовый APK будет здесь:**
```
android/app/build/outputs/apk/release/app-release.apk
```

**Размер APK:** примерно 15-30 МБ (зависит от размера вашего приложения).

---

## 📱 ЧАСТЬ 2: Регистрация в RuStore Console

### Шаг 1: Создание аккаунта разработчика

1. Перейдите на: https://console.rustore.ru/
2. Войдите через VK ID или создайте аккаунт
3. Заполните данные разработчика:
   - ФИО или название компании
   - ИНН (если ИП или компания)
   - Email для связи
   - Телефон

**Важно:** Для монетизации (платные подписки) нужен статус ИП или ООО. Для бесплатного приложения достаточно физлица.

### Шаг 2: Верификация аккаунта

- Подтвердите email (придёт письмо)
- Подтвердите телефон (придёт SMS)
- Дождитесь проверки модерацией (1-3 рабочих дня)

---

## 📦 ЧАСТЬ 3: Создание карточки приложения

### Шаг 1: Создать новое приложение

1. В RuStore Console нажмите **"Создать приложение"**
2. Выберите **"Android приложение"**
3. Загрузите файл `app-release.apk` (из Части 1, Шаг 8)

### Шаг 2: Заполнить основную информацию

**Название приложения:**
```
Studyfay
```

**Короткое описание (до 80 символов):**
```
ИИ-ассистент для студентов: лекции, расписание, подготовка к экзаменам
```

**Полное описание (до 4000 символов):**
```
Studyfay — умный помощник для студентов

🤖 ИИ-ассистент по учебным материалам
• Загружайте лекции в PDF, DOCX, TXT
• Задавайте вопросы по материалам — ИИ ответит простым языком
• Автоматическое распознавание текста и анализ документов
• Генерация компактных шпаргалок для быстрого повторения

📚 Библиотека материалов
• Храните все лекции и конспекты в одном месте
• Умный поиск по содержимому документов
• Автоматическое определение предмета и темы

📅 Планирование учёбы
• Расписание занятий с уведомлениями
• Список задач с дедлайнами
• Синхронизация между устройствами

🎯 Подготовка к экзаменам
• Прогнозы вероятных вопросов на основе материалов
• Персональные рекомендации по повторению
• Отслеживание прогресса подготовки

💳 Подписка
• Бесплатный пробный период 2 дня
• Доступ ко всем функциям ИИ-ассистента
• Безлимитная загрузка материалов
• Оплата через Т-Банк (бывший Тинькофф)

Сделано студентами для студентов!
```

**Категория:**
- Образование

**Возрастной рейтинг:**
- 12+ (или 6+, если нет спорного контента)

**Email разработчика:**
```
rekrutiv@gmail.com
```

**Сайт приложения (опционально):**
```
https://app.studyfay.ru
```

**Политика конфиденциальности (обязательно!):**
```
https://app.studyfay.ru/privacy
```
*(Если у вас нет страницы — создайте простую HTML-страницу с политикой)*

---

### Шаг 3: Загрузить скриншоты и иконку

**Требования к скриншотам:**
- Минимум 2 скриншота, максимум 8
- Формат: PNG или JPEG
- Размер: 1080x1920 (9:16) или 720x1280
- Без рамок телефона, только UI приложения

**Требования к иконке:**
- Размер: 512x512 пикселей
- Формат: PNG с прозрачным фоном
- Ваша иконка уже есть в `public/icon-512x512.png`

**Где взять скриншоты:**
1. Откройте приложение в браузере на телефоне
2. Сделайте скриншоты основных экранов:
   - Главный экран (расписание)
   - ИИ-ассистент
   - Библиотека материалов
   - Страница подписки
3. Обрежьте скриншоты до размера экрана (без статус-бара)

---

### Шаг 4: Настройка монетизации

**Тип распространения:**
- Бесплатное (с встроенными покупками)

**Встроенные покупки:**
- Да (подписка через Т-Банк)

**Описание покупок:**
```
Подписка Studyfay Premium:
- 1 месяц: 199₽
- 3 месяца: 499₽
- 6 месяцев: 999₽

Дополнительные токены для ИИ:
- 25,000 токенов: 99₽
- 50,000 токенов: 179₽
- 100,000 токенов: 299₽
```

---

## 🔍 ЧАСТЬ 4: Модерация и публикация

### Шаг 1: Отправка на модерацию

После заполнения всех полей нажмите **"Отправить на модерацию"**.

**Что проверяют модераторы:**
1. APK установился и запустился
2. Нет крашей и критических багов
3. Описание соответствует функциям приложения
4. Нет запрещённого контента
5. Политика конфиденциальности доступна

**Сроки модерации:** 1-3 рабочих дня

---

### Шаг 2: Исправление замечаний (если есть)

Если модерация отклонила приложение:
1. Прочитайте причину отклонения в личном кабинете
2. Исправьте указанные проблемы
3. Загрузите новый APK (увеличьте `versionCode` в `build.gradle`)
4. Отправьте на повторную модерацию

**Частые причины отклонения:**
- Приложение не запускается
- Нет политики конфиденциальности
- Описание не соответствует функциям
- Скриншоты низкого качества
- Крах при первом запуске

---

### Шаг 3: Публикация

После одобрения модерацией приложение автоматически появится в RuStore.

**Ссылка на приложение будет вида:**
```
https://apps.rustore.ru/app/dev.poehali.studyfay
```

---

## 🔄 Обновление приложения

### Когда нужно обновить APK:

1. Исправили баги
2. Добавили новые функции
3. Обновили дизайн

### Как обновить:

1. **Внесите изменения в код**
2. **Увеличьте версию в `android/app/build.gradle`:**
   ```gradle
   versionCode 2  // было 1
   versionName "1.0.1"  // было 1.0.0
   ```
3. **Пересоберите APK:**
   ```bash
   npm run build
   npx cap sync
   cd android
   ./gradlew assembleRelease
   ```
4. **Загрузите новый APK в RuStore Console**
5. **Отправьте на модерацию**

---

## ❓ Частые вопросы

### 1. Нужно ли платить за публикацию в RuStore?
**Ответ:** Нет, публикация бесплатна.

### 2. Можно ли опубликовать приложение без ИП?
**Ответ:** Да, можно как физлицо. Но для монетизации (подписки) лучше оформить ИП.

### 3. Сколько занимает модерация?
**Ответ:** Обычно 1-3 рабочих дня. В пиковые периоды до 5 дней.

### 4. Что делать, если потерял файл keystore?
**Ответ:** Без keystore вы не сможете обновить приложение. Придётся создать новое приложение с новым package ID.

### 5. Можно ли изменить package ID после публикации?
**Ответ:** Нет. Package ID (dev.poehali.studyfay) — это уникальный идентификатор приложения навсегда.

### 6. Как пользователи будут оплачивать подписку?
**Ответ:** Через встроенную систему Т-Банк (уже реализовано в коде). RuStore не требует своей платёжной системы.

### 7. Будет ли работать push-уведомления?
**Ответ:** Да, если подключить Firebase Cloud Messaging (опционально).

---

## 📞 Поддержка

**RuStore документация:**
- https://help.rustore.ru/rustore/for_developers

**Техподдержка RuStore:**
- Email: dev@rustore.ru
- Telegram: @rustore_support

**Наши контакты:**
- Email: rekrutiv@gmail.com
- Сообщество: https://t.me/+QgiLIa1gFRY4Y2Iy

---

## ✅ Чек-лист перед публикацией

- [ ] Node.js и npm установлены
- [ ] Android Studio установлена
- [ ] Capacitor установлен (`npm install @capacitor/...`)
- [ ] `capacitor.config.ts` настроен
- [ ] Веб-приложение собрано (`npm run build`)
- [ ] Android платформа добавлена (`npx cap add android`)
- [ ] Keystore создан и сохранён в безопасном месте
- [ ] `build.gradle` настроен с подписью
- [ ] APK собран (`./gradlew assembleRelease`)
- [ ] APK протестирован на реальном устройстве
- [ ] Аккаунт в RuStore Console создан и верифицирован
- [ ] Иконка 512x512 готова
- [ ] Минимум 2 скриншота готовы
- [ ] Описание и категория заполнены
- [ ] Политика конфиденциальности опубликована
- [ ] Приложение отправлено на модерацию

---

**Успехов с публикацией! 🚀**
